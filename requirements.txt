# Core
httpx>=0.27
tenacity>=8.2          # retry/backoff
python-dotenv>=1.0
pydantic>=2.7          # schema validation

# Spreadsheet output
openpyxl>=3.1          # .xlsx writer

# Google Sheets sink (optional — only needed for --sink sheets)
gspread>=6.1
google-auth>=2.30

# Free/local scraping backend (optional — only for --provider playwright)
playwright>=1.45
